# Final policy

`autoprune_llava15_mme_k32_anchor.yaml` is the canonical TPDSL anchor policy used for the main Qwen-Plus result (MME Perception 1413.46 at K=32). Pair it with `openevolve/policies/final/autoprune_llava15_mme_k32_merge.yaml`.
